name: flutter_application_linux
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  grpc: ^3.0.0  
  protobuf: ^3.1.0
  async: ^2.8.21

  english_words: ^4.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  intl: ^0.17.0

flutter:
  uses-material-design: true
  assets:
    - assets/Urban-Ray-logo_dark.png
    - assets/paramaters_config.json